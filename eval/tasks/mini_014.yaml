task_id: mini_014
repo: repos/mini_014
issue: |
  The report generator is producing incorrect totals. Instead of showing a
  summed total per category, the output contains one entry per transaction,
  as if no aggregation is happening at all. The formatted report output does
  not match the expected category-level breakdown.

  For example, a batch of 10 food transactions should produce a single
  "food" total, but instead the report contains 10 separate line items.
  Please investigate the report formatting and aggregation logic.
test_cmd: pytest -q --tb=short
time_limit_s: 360
max_iterations: 5
difficulty: hard
tags:
  - multi-file
  - wrong-file-trap
  - reflection-critical
  - large-repo
