task_id: mini_020
repo: repos/mini_020
issue: |
  The student score analysis pipeline is reporting incorrect normalized scores
  for most students. Input records are parsed and validated without errors. The
  scores are systematically wrong — the deviation is not a fixed offset and varies
  across different configurations, which suggests the computation is using the
  wrong reference value.

  The output structure and student ID assignments look correct. The issue is
  in how the score is normalized, not in data loading or validation.
test_cmd: pytest test_regression.py -v
time_limit_s: 360
max_iterations: 5
difficulty: hard
tags:
  - multi-file
  - wrong-file-trap
  - reflection-critical
  - generic-test-names
  - normalization
