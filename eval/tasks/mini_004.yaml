task_id: mini_004
repo: repos/mini_004
issue: |
  Records are sometimes lost when reading JSONL files that do not end with a
  trailing newline. Fix read_records() so it correctly handles input that has
  no newline at the end of the last record.
test_cmd: pytest -q --tb=short
time_limit_s: 360
max_iterations: 5
difficulty: hard
tags:
  - multi-file
  - jsonl
  - hidden-writer-bug
