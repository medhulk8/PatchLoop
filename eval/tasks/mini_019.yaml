task_id: mini_019
repo: repos/mini_019
issue: |
  The inventory shrinkage analysis pipeline is producing incorrect shrinkage
  percentages for most items. Input records load and validate without errors.
  The shrinkage values are systematically wrong — the deviation is not a fixed
  amount and varies depending on the item's stock levels, which suggests the
  computation is using the wrong reference quantity.

  The output structure and item ID assignments look correct. The issue is
  in how shrinkage is calculated, not in data loading or validation.
test_cmd: pytest test_regression.py -v
time_limit_s: 360
max_iterations: 5
difficulty: hard
tags:
  - multi-file
  - wrong-file-trap
  - reflection-critical
  - generic-test-names
  - shrinkage
