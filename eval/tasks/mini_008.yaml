task_id: mini_008
repo: repos/mini_008
issue: |
  group_rows() does not merge values when the same key appears in
  non-contiguous rows. Fix it so all rows with the same key are grouped
  together regardless of their position in the input.
test_cmd: pytest -q --tb=short
time_limit_s: 360
max_iterations: 5
difficulty: medium
tags:
  - order-preservation
  - groupby
  - hidden-invariant
