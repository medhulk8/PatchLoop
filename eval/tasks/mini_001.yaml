task_id: mini_001
repo: repos/mini_001
issue: |
  The retry() function is supposed to retry only on TransientError,
  but it seems to be retrying even when a PermanentError is raised.
  PermanentError should propagate immediately without any retries.
  The test suite confirms this expected behavior.
test_cmd: pytest -q --tb=short
time_limit_s: 360
max_iterations: 5
difficulty: medium
tags:
  - exception-handling
  - control-flow
