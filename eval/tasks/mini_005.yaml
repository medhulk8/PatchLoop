task_id: mini_005
repo: repos/mini_005
issue: |
  Calling merge_config() with a nested key override removes sibling keys from
  the same nested object. For example, overriding only "host" inside "db"
  causes "port" to disappear. Fix merge_config() to preserve all existing keys.
test_cmd: pytest -q --tb=short
time_limit_s: 360
max_iterations: 5
difficulty: hard
tags:
  - recursion
  - mutation
  - config
