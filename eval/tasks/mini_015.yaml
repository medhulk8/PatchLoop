task_id: mini_015
repo: repos/mini_015
issue: |
  The event pipeline is producing incorrect summary reports. Category
  totals and priority ranges in the output do not match what the input
  records contain.

  The report format itself looks fine — the issue is that the numeric
  values being reported are wrong. Please investigate and fix the root
  cause.
test_cmd: pytest -q --tb=short
time_limit_s: 360
max_iterations: 5
difficulty: hard
tags:
  - multi-file
  - cascade-bug
  - wrong-file-trap
  - reflection-critical
  - generic-test-names
